      DOUBLE PRECISION FUNCTION DLUM()
      IMPLICIT NONE
      INTEGER NFKSPROCESS
      COMMON/C_NFKSPROCESS/NFKSPROCESS
      SELECT CASE(NFKSPROCESS)
      CASE(1)
      CALL DLUM_1(DLUM)
      CASE(2)
      CALL DLUM_1(DLUM)
      CASE(3)
      CALL DLUM_1(DLUM)
      CASE(4)
      CALL DLUM_1(DLUM)
      CASE(5)
      CALL DLUM_2(DLUM)
      CASE(6)
      CALL DLUM_3(DLUM)
      CASE(7)
      CALL DLUM_NATIVE_1_3(DLUM)
      CASE(8)
      CALL DLUM_NATIVE_1_4(DLUM)
      CASE DEFAULT
      STOP 'Invalid native PDF sector'
      END SELECT
      CALL MC_FILTER_NATIVE_LUM(DLUM)
      END
