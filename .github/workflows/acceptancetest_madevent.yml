# This is a basic workflow to help you get started with Actions

name: running acceptance test (madevent)
# Controls when the workflow will run
#    branches: [ main LTS ]
on:
  # Triggers the workflow on push or pull request events but only for the 3.4.0 branch
  push:
    paths-ignore:
      - 'docs/**'
#      - '.github/**'

  pull_request:
    types: [opened, synchronize, reopened]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
    inputs:
      message:
        description: 'running acceptance test'
        required: true

env:
  commitmsg: ${{ github.event.head_commit.message }}
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  acceptancetest_0:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all


      # Runs a set of commands using the runners shell
      - name: test one of the test test_hepmc_decay test_lhe_none_decay test_madspin_spin_only test_madspin_mixed_flavor_decay_log_summary test_madspin_wplus_all_all_flavor_balance test_madspin_wplus_all_all_flavor_balance_2to1
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_hepmc_decay -pA -t0 -l INFO
            ./tests/test_manager.py test_lhe_none_decay -pA -t0 -l INFO
            ./tests/test_manager.py test_madspin_spin_only -pA -t0 -l INFO
            ./tests/test_manager.py test_madspin_mixed_flavor_decay_log_summary -pA -t0 -l INFO
            ./tests/test_manager.py test_madspin_wplus_all_all_flavor_balance -pA -t0 -l INFO
            ./tests/test_manager.py test_madspin_wplus_all_all_flavor_balance_2to1 -pA -t0 -l INFO



  acceptancetest_10:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache
      # Runs a set of commands using the runners shell
      - name: test one of the test test_complex_mass_SA/ DYj has no assymetry
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_DY_onejet -pA -t0 -l INFO


  acceptancetest_17:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_leshouche_sextet_diquarks
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_leshouche_sextet_diquarks -pA -t0 -l INFO



  acceptancetest_19:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_madevent_decay_chain
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madevent_decay_chain -pA -t0 -l INFO



  acceptancetest_20:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_madevent_subproc_group
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madevent_subproc_group -pA -t0 -l INFO



  acceptancetest_21:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_madevent_subproc_group_decay_chain
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madevent_subproc_group_decay_chain -pA -t0 -l INFO



  acceptancetest_22:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_madevent_subproc_group_symmetry
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madevent_subproc_group_symmetry -pA -t0 -l INFO



  acceptancetest_23:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_madevent_triplet_diquarks
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madevent_triplet_diquarks -pA -t0 -l INFO



  acceptancetest_24:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache


      # Runs a set of commands using the runners shell
      - name: test one of the test test_madevent_ufo_aloha
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madevent_ufo_aloha -pA -t0 -l INFO



  acceptancetest_25:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_output_madevent_directory
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_output_madevent_directory -pA -t0 -l INFO



  acceptancetest_35:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_ungroup_decay
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_ungroup_decay -pA -t0 -l INFO



  acceptancetest_37:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache
      - uses: ./.github/actions/restore_heptools

      - name: test one of the test test_loop_improved_reweighting
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py  test_w_production_with_ms_decay test_add_time_of_flight -pA -t0 -l INFO


  acceptancetest_38:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_mass_reweighting
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_mass_reweighting -pA -t0 -l INFO


  test_oneloop_reweighting:
    # The type of runner that the job will run on
    runs-on: ${{ matrix.os }}
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    strategy:
      matrix:
        os: [ubuntu-24.04]

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache
      - uses: ./.github/actions/restore_all

      - name: Install Meson and Ninja
        run: |
           sudo apt-get update
           sudo apt-get install -y meson ninja-build


      # Runs a set of commands using the runners shell
      - name: test one of the test test_oneloop_reweighting
        run: |
            cd $GITHUB_WORKSPACE
      # Runs a set of commands using the runners shell
      - name: test one of the test test_oneloop_reweighting
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_oneloop_reweighting -pA -t0 -l INFO


  acceptancetest_42:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_scan_reweighting
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_scan_reweighting -pA -t0 -l INFO



  acceptancetest_43:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_complex_mass_scheme
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_complex_mass_scheme -pA -t0 -l INFO



  acceptancetest_44:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_creating_matched_plot
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_creating_matched_plot -pA -t0 -l INFO



  acceptancetest_45:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_customised_madevent_via_run_card
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_customised_madevent_via_run_card -pA -t0 -l INFO



  acceptancetest_46:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_e_e_collision
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_e_e_collision -pA -t0 -l INFO



  acceptancetest_47:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_e_p_collision
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_e_p_collision -pA -t0 -l INFO



  acceptancetest_48:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5

      # Runs a set of commands using the runners shell
      - name: test one of the test test_eft_running
        run: |
            cd $GITHUB_WORKSPACE
            cp input/.mg5_configuration_default.txt input/mg5_configuration.txt
            echo "install RunningCoupling" > cmd
            ./bin/mg5_aMC cmd
            ./tests/test_manager.py test_eft_running -pA -t0 -l INFO



  acceptancetest_49:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_eva_collision
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_eva_collision test_eva_oldrelease_collision test_ieva_collision -pA -t0 -l INFO



  acceptancetest_50:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_group_subprocess
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_group_subprocess -pA -t0 -l INFO



  acceptancetest_51:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_madevent_ptj_bias
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madevent_ptj_bias -pA -t0 -l INFO



  acceptancetest_52:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_madspin_gridpack
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madspin_gridpack -pA -t0 -l INFO



  acceptancetest_53:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache
      - uses: ./.github/actions/restore_ufomodel

      # Runs a set of commands using the runners shell
      - name: test one of the test test_width_computation
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_width_computation -pA -t0 -l INFO



  acceptancetest_54:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_width_nlocomputation
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_width_nlocomputation -pA -t0 -l INFO



  acceptancetest_55:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_width_scan
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_width_scan -pA -t0 -l INFO



  acceptancetest_56:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_run_fromP
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_run_fromP -pA -t0 -l INFO


  acceptancetest_contur:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache
      - uses: ./.github/actions/restore_heptools

      - name: run the test
        run: |
            #./tests/test_manager.py test_contur_from_file test_rivet_from_file -pA -t0 -l INFO
            PYTHONPATH=$PYTHONPATH:/home/runner/.cache/HEPtools/contur/python3.10:/home/runner/.cache/HEPTools/rivet/local/lib/python3.10/dist-packages/:/home/runner/.cache/HEPTools/yoda/local/lib/python3.10/dist-packages/ LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/home/runner/.cache/HEPTools/lib ./tests/test_manager.py  test_rivet_from_file -pA -t0 -l INFO

  acceptancetest_contur2:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache
      - uses: ./.github/actions/restore_heptools

      - name: run the test
        run: |
            PYTHONPATH=$PYTHONPATH:/home/runner/.cache/HEPtools/contur/python3.10 LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/home/runner/.cache/HEPTools/lib ./tests/test_manager.py test_rivet_from_file -pA -t0 -l INFO


  acceptancetest_59:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_decay_width_nlo_model
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_decay_width_nlo_model -pA -t0 -l INFO



  acceptancetest_60:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_generation_from_file_1
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_generation_from_file_1 -pA -t0 -l INFO




  acceptancetest_100_2:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache
      - uses: ./.github/actions/restore_heptools

      # Runs a set of commands using the runners shell
      - name: test one of the test test_loop_improved_reweighting
        run: |
            cd $GITHUB_WORKSPACE
            sudo apt install pigz
            ./tests/test_manager.py  test_loop_improved_reweighting -pA -t0 -l INFO --debug

  acceptancetest_101:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      # Runs a set of commands using the runners shell
      - name: test one of the test test_loop_improved_reweighting
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py  test_nlo_output2 -pA -t0 -l INFO

  acceptancetest_102:
    # The type of runner that the job will run on

    runs-on: ${{ matrix.os }}
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    strategy:
      matrix:
        os: [ubuntu-24.04]

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache
      - uses: ./.github/actions/restore_heptools
      - name: Install Meson and Ninja
        run: |
           sudo apt-get update
           sudo apt-get install -y meson ninja-build

      # Runs a set of commands using the runners shell
      - name: test one of the test test_loop_improved_reweighting
        run: |
            cd $GITHUB_WORKSPACE
            export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/home/runner/.cache/HEPtools/lhapdf6_py3/lib
            export PYTHONPATH=$PYTHONPATH:~/.cache/HEPtools/lhapdf6_py3/lib/python3.12/dist-packages
            export PYTHONPATH=$PYTHONPATH:~/.cache/HEPtools/lhapdf6_py3/lib/python3.10/dist-packages
            ./tests/test_manager.py  test_nlo_reweighting -pA -t0 -l INFO

  acceptancetest_103_polarization:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5

      # Runs a set of commands using the runners shell
      - name: test one of the test test_polarization_top_decay
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_polarization_top_decay -pA -t0 -l INFO

  acceptancetest_heft:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5 

      # Runs a set of commands using the runners shell
      - name: test one of the test test_loop_improved_reweighting
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_generation_heft -pA -t0 -l INFO

  acceptancetest_flavor:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      # Runs a set of commands using the runners shell
      - name: test flavor consistency and decay-chain symmetry factor
        run: |
            cd $GITHUB_WORKSPACE
            cp input/.mg5_configuration_default.txt input/mg5_configuration.txt
            ./tests/test_manager.py test_flavor_grouping_consistency_mlm test_flavor_grouping_consistency test_decay_chain_symmetry_factor -pA -t0 -l INFO


  acceptancetest_uncovered_merged:
    # Previously uncovered acceptance tests in tests/acceptance_tests/test_cmd.py
    # related to the merged/ufo_aloha pipelines and decay-chain ordering
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      - name: test cmd uncovered merged/ufo_aloha/standalone tests
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_decay_chain_identical_particle_outoforder test_madevent_ufo_aloha_merged -pA -t0 -l INFO


  acceptancetest_uncovered_madevent:
    # Previously uncovered acceptance tests in tests/acceptance_tests/test_cmd_madevent.py
    # Includes test_madevent_dy3j_mlm after reopening/owning stdout safely.
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      - name: test madevent uncovered acceptance tests
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_flavor_grouping_consistency_width test_madevent_dy3j_mlm -pA -t0 -l INFO


  acceptancetest_madevent_vbf_merged_flavor:
    # Previously uncovered acceptance test in tests/acceptance_tests/test_cmd_madloop.py
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - name: test madevent_with_merged_flavor
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madevent_merged_flavor_uq test_madevent_flavor_zjj -pA -t0 -l INFO

  acceptancetest_madevent_flavor_zud:
    # Flavor-grouped u q > Z u q QCD=0 tests (grouped and ungrouped).
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - name: test madevent flavor zud (grouped and nogroup)
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madevent_flavor_zud test_madevent_flavor_zud_nogroup -pA -t0 -l INFO

  acceptancetest_density_interface:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5 
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_density_mode_user_interface
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_density_mode_user_interface -pA -t0 -l INFO


  acceptancetest_density_ttbar:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5 
      - uses: ./.github/actions/restore-pip-cache
      # Runs a set of commands using the runners shell
      - name: test one of the test test_density_mode_ttbar
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_density_mode_ttbar -pA -t0 -l DEBUG

  acceptancetest_density_wpwm:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5 
      - uses: ./.github/actions/restore-pip-cache    
      # Runs a set of commands using the runners shell
      - name: test one of the test test_density_mode_wpwm
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_density_mode_wpwm -pA -t0 -l INFO

  acceptancetest_density_decay1:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5 
      - uses: ./.github/actions/restore-pip-cache
      # Runs a set of commands using the runners shell
      - name: test one of the test test_density_mode_decay1
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_density_mode_decay1 -pA -t0 -l INFO

  acceptancetest_density_decay2:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5 
      - uses: ./.github/actions/restore-pip-cache
      # Runs a set of commands using the runners shell
      - name: test one of the test test_density_mode_decay2
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_density_mode_decay2 -pA -t0 -l INFO

  acceptancetest_density_tttbartbar:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      # Runs a set of commands using the runners shell
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5 
      - uses: ./.github/actions/restore-pip-cache
      - name: test one of the test test_density_mode_doublettbar
        run: |
            cd $GITHUB_WORKSPACE
            cp input/.mg5_configuration_default.txt input/mg5_configuration.txt
            ./tests/test_manager.py test_density_mode_doublettbar -pA -t0 -l INFO
