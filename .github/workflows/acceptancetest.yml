# This is a basic workflow to help you get started with Actions

name: running acceptance test (rest)
# Controls when the workflow will run
#    branches: [ main LTS ]
on:
  # Triggers the workflow on push or pull request events but only for the 3.4.0 branch
  push:
    paths-ignore:
      - 'docs/**'
#      - '.github/**'

  pull_request:
    types: [opened, synchronize, reopened]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
    inputs:
      message:
        description: 'running acceptance test'
        required: true

env:
  commitmsg: ${{ github.event.head_commit.message }}
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  acceptancetest_5:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache
      - uses: ./.github/actions/restore_ufomodel
      # Runs a set of commands using the runners shell
      - name: test one of the test test_config
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_config -pA -t0 -l INFO
            ./tests/test_manager.py test_draw -pA -t0 -l INFO
            ./tests/test_manager.py test_generate -pA -t0 -l INFO
            ./tests/test_manager.py test_import_model -pA -t0 -l INFO
            ./tests/test_manager.py test_check_generate_optimize -pA -t0 -l INFO


  acceptancetest_10:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache
      # Runs a set of commands using the runners shell
      - name: test one of the test test_complex_mass_SA/ DYj has no assymetry
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_complex_mass_SA -pA -t0 -l INFO


  acceptancetest_11:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache
      # Runs a set of commands using the runners shell
      - name: test one of the test test_custom_propa
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_custom_propa -pA -t0 -l INFO



  acceptancetest_12:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_define_order
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_define_order -pA -t0 -l INFO



  acceptancetest_13:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_import_banner_command
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_import_banner_command -pA -t0 -l INFO



  acceptancetest_14:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_invalid_operations_for_add
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_invalid_operations_for_add -pA -t0 -l INFO



  acceptancetest_15:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_invalid_operations_for_generate
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_invalid_operations_for_generate -pA -t0 -l INFO



  acceptancetest_16:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_invalid_operations_for_output
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_invalid_operations_for_output -pA -t0 -l INFO



  acceptancetest_18:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_load_feynman
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_load_feynman -pA -t0 -l INFO



  acceptancetest_26:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_output_standalone_directory
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_output_standalone_directory -pA -t0 -l INFO



  acceptancetest_28:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_read_madgraph4_proc_card
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_read_madgraph4_proc_card -pA -t0 -l INFO



  acceptancetest_29:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_save_load
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_save_load -pA -t0 -l INFO



  acceptancetest_30:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_standalone_cpp
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_standalone_cpp -pA -t0 -l INFO



  acceptancetest_31:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_standalone_cpp_output
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_standalone_flavor_mask test_standalone_wwjj test_standalone_cpp_output test_standalone_merged_flavor_uq_zuq -pA -t0 -l INFO



  acceptancetest_32:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_standalone_cpp_output_consistency
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_standalone_cpp_output_consistency -pA -t0 -l INFO
            ./tests/test_manager.py test_standalone_mg7_vs_cpp -pA -t0 -l INFO



  acceptancetest_33:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_ufo_aloha
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_ufo_aloha -pA -t0 -l INFO


  acceptancetest_spin2_standalone:
    # Regression test for spin-2 wavefunction storage in standalone
    # Fortran output (loop_smgrav: p p > w+ y). Guards against the
    # TYPE(ALOHA)/TYPE(ALOHA2D) layout mismatch that overruns the
    # caller's wavefunction slot when TXXXXX writes 16 complex into
    # a 4-complex aloha element.
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      - name: test test_standalone_spin2_loop_smgrav
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_standalone_spin2_loop_smgrav -pA -t0 -l INFO



  acceptancetest_34:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_ufo_standard_sm
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_ufo_standard_sm -pA -t0 -l INFO



  acceptancetest_36:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_v4_heft
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_v4_heft -pA -t0 -l INFO

#
#  installpy8:
#    # The type of runner that the job will run on
#    runs-on: ubuntu-24.04
#
#    # Steps represent a sequence of tasks that will be executed as part of the job
#    steps:
#      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
#      - uses: actions/checkout@v4
#      - uses: ./.github/actions/checkout_mg5
#      - uses: ./.github/actions/restore-pip-cache
#
#      # Runs a set of commands using the runners shell
#      - name: test one of the test test_loop_improved_reweighting
#        run: |
#            cd $GITHUB_WORKSPACE
#            echo "install lhapdf6" > cmd
#            echo "install pythia8" >> cmd
#            ./bin/mg5_aMC cmd
#
#      # Runs a set of commands using the runners shell

  acceptancetest_48:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5

      # Runs a set of commands using the runners shell
      - name: test one of the test test_eft_running
        run: |
            cd $GITHUB_WORKSPACE
            cp input/.mg5_configuration_default.txt input/mg5_configuration.txt
            echo "install RunningCoupling" > cmd
            ./bin/mg5_aMC cmd
            ./tests/test_manager.py test_eft_running_nlo -pA -t0 -l INFO



  acceptancetest_65:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_tt_semi
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_tt_semi -pA -t0 -l INFO



  acceptancetest_66:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_zh
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_zh -pA -t0 -l INFO



  acceptancetest_67:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_mssm_equivalence
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_mssm_equivalence -pA -t0 -l INFO



  acceptancetest_68:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_ufomodel

      # Runs a set of commands using the runners shell
      - name: test one of the test test_sm_equivalence
        run: |
            cd $GITHUB_WORKSPACE
            cp input/.mg5_configuration_default.txt input/mg5_configuration.txt
            echo "f2py_compiler = $(which f2py)" >> input/mg5_configuration.txt
            ./tests/test_manager.py test_sm_equivalence -pA -t0 -l INFO

  acceptancetest_69:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_ufomodel

      # Runs a set of commands using the runners shell
      - name: test one of the test test_sm_equivalence
        run: |
            cd $GITHUB_WORKSPACE
            cp input/.mg5_configuration_default.txt input/mg5_configuration.txt
            echo "f2py_compiler = $(which f2py)" >> input/mg5_configuration.txt
            ./tests/test_manager.py test_all -pA -t0 -l DEBUG --debug



  acceptancetest_71:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      # Runs a set of commands using the runners shell
      - name: test one of the test test_ML_check_brs_gd_gd
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_ML_check_brs_gd_gd -pA -t0 -l INFO
            ./tests/test_manager.py test_generate_output_semicolon_preserves_loop_process -pA -t0 -l INFO



  acceptancetest_72:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      # Runs a set of commands using the runners shell
      - name: test one of the test test_ML_check_cms_aem_emvevex
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_ML_check_cms_aem_emvevex -pA -t0 -l INFO



  acceptancetest_73:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      # Runs a set of commands using the runners shell
      - name: test one of the test test_ML_check_cms_al_lvlvlx_LO
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_ML_check_cms_al_lvlvlx_LO -pA -t0 -l INFO



  acceptancetest_74:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      # Runs a set of commands using the runners shell
      - name: test one of the test test_ML_check_full_epem_ttx
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_ML_check_full_epem_ttx -pA -t0 -l INFO



  acceptancetest_75:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all


      # Runs a set of commands using the runners shell
      - name: test one of the test test_ML_check_profile_epem_ttx
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_ML_check_profile_epem_ttx -pA -t0 -l INFO



  acceptancetest_76:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      # Runs a set of commands using the runners shell
      - name: test one of the test test_ML_check_timing_epem_ttx
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_ML_check_timing_epem_ttx -pA -t0 -l INFO



  acceptancetest_78:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test testIO_check_html_long_process_strings
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_check_html_long_process_strings -pA -t0 -l INFO



  acceptancetest_79:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      # Runs a set of commands using the runners shell
      - name: test one of the test test_amcantlo_script
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_amcantlo_script -pA -t0 -l INFO



  acceptancetest_80:
    # The type of runner that the job will run on
    runs-on: ${{ matrix.os }}
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    strategy:
      matrix:
        os: [ubuntu-24.04]

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      # Runs a set of commands using the runners shell
      - name: test one of the test test_amcatnlo_from_file
        run: |
            cd $GITHUB_WORKSPACE
            echo "set acknowledged_v3.1_syntax True --global" > cmd
            ./bin/mg5_aMC cmd
            ./tests/test_manager.py test_amcatnlo_from_file -pA -t0 -l INFO


  acceptancetest_81:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_calculate_xsect_lo
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_calculate_xsect_lo -pA -t0 -l INFO



  acceptancetest_82:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      # Runs a set of commands using the runners shell
      - name: test one of the test test_calculate_xsect_nlo
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_calculate_xsect_nlo -pA -t0 -l INFO



  acceptancetest_83:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      # Runs a set of commands using the runners shell
      - name: test one of the test test_check_singletop_fastjet
        run: |
            cd $GITHUB_WORKSPACE
            sudo apt install -y libtirpc-dev
            ./tests/test_manager.py test_check_singletop_fastjet -pA -t0 -l INFO


            #  acceptancetest_85:
            #    # The type of runner that the job will run on
            #    runs-on: ubuntu-24.04
            #
            #    # Steps represent a sequence of tasks that will be executed as part of the job
            #    steps:
            #      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
            #      - uses: actions/checkout@v4
            #      - uses: ./.github/actions/checkout_mg5
            #      - uses: ./.github/actions/restore-pip-cache
            #
            #      # Runs a set of commands using the runners shell
            #      - name: test one of the test test_gen_evt_onlygen
            #        run: |
            #            cd $GITHUB_WORKSPACE
            #            ./tests/test_manager.py test_gen_evt_onlygen -pA -t0 -l INFO
            #


  acceptancetest_emela:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      - name: test one of the test test_generate_eett_lo_emela_clusterpath
        run: |
            ./tests/test_manager.py test_generate_eett_lo_emela_clusterpath -pA -t0 -l INFO


  acceptancetest_emela2:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      - name: test one of the test test_generate_eett_lo_emela_clusterpath
        run: |
            ./tests/test_manager.py test_generate_eett_nlo_emela_bs -pA -t0 -l INFO


  acceptancetest_emela3:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      - name: test one of the test test_generate_eett_lo_emela_clusterpath
        run: |
            ./tests/test_manager.py test_generate_eeww_nlo_emela_noph -pA -t0 -l INFO

  acceptancetest_emela4:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      - name: test one of the test test_generate_eett_lo_emela_clusterpath
        run: |
            ./tests/test_manager.py test_generate_eeww_nlo_emela_wph -pA -t0 -l INFO

  acceptancetest_88:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      # Runs a set of commands using the runners shell
      - name: test one of the test test_generate_eett_nlo_qcd_noisr
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_generate_eett_nlo_qcd_noisr -pA -t0 -l INFO



            #  acceptancetest_91:
            #    # The type of runner that the job will run on
            #    runs-on: ubuntu-24.04
            #
            #    # Steps represent a sequence of tasks that will be executed as part of the job
            #    steps:
            #      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
            #      - uses: actions/checkout@v4
            #      - uses: ./.github/actions/checkout_mg5
            #      - uses: ./.github/actions/restore-pip-cache
            #
            #      # Runs a set of commands using the runners shell
            #      - name: test one of the test test_generate_events_lo_hw6_stdhep
            #        run: |
            #            cd $GITHUB_WORKSPACE
            #            ./tests/test_manager.py test_generate_events_lo_hw6_stdhep -pA -t0 -l INFO
            #


            #  acceptancetest_92:
            #    # The type of runner that the job will run on
            #    runs-on: ubuntu-24.04
            #
            #    # Steps represent a sequence of tasks that will be executed as part of the job
            #    steps:
            #      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
            #      - uses: actions/checkout@v4
            #      - uses: ./.github/actions/checkout_mg5
            #      - uses: ./.github/actions/restore-pip-cache
            #
            #      # Runs a set of commands using the runners shell
            #      - name: test one of the test test_generate_events_lo_py6_stdhep
            #        run: |
            #            cd $GITHUB_WORKSPACE
            #            ./tests/test_manager.py test_generate_events_lo_py6_stdhep -pA -t0 -l INFO
            #


  acceptancetest_93:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      # Runs a set of commands using the runners shell
      - name: test one of the test test_generate_events_nlo_hw6_split
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_generate_events_nlo_hw6_split -pA -t0 -l INFO



  acceptancetest_94:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      # Runs a set of commands using the runners shell
      - name: test one of the test test_generate_taggedph_nloew
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_generate_taggedph_nloew -pA -t0 -l INFO



  acceptancetest_95:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      # Runs a set of commands using the runners shell
      - name: test one of the test test_madspin_LOonly
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madspin_LOonly -pA -t0 -l INFO


  acceptancetest_96:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      # Runs a set of commands using the runners shell
      - name: test one of the test test_madspin_ON_and_onshell_atNLO
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madspin_ON_and_onshell_atNLO -pA -t0 -l INFO



  acceptancetest_97:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      # Runs a set of commands using the runners shell
      - name: test one of the test test_raise_invalid_path_hwpp
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_raise_invalid_path_hwpp -pA -t0 -l INFO



  acceptancetest_98:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      # Runs a set of commands using the runners shell
      - name: test one of the test test_raise_invalid_path_py8
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_raise_invalid_path_py8 -pA -t0 -l INFO



  acceptancetest_99:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      # Runs a set of commands using the runners shell
      - name: test one of the test test_split_evt_gen
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_split_evt_gen -pA -t0 -l INFO


acceptancetest_100:
  runs-on: ubuntu-24.04
  if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
  steps:
    - uses: actions/checkout@v4
    - uses: ./.github/actions/checkout_mg5 
    - uses: ./.github/actions/restore-pip-cache
    - name: test one of the test testIO_gnuplot_histo_output
      run: |
          cd $GITHUB_WORKSPACE
          ./tests/test_manager.py testIO_gnuplot_histo_output -pA -t0 -l INFO

acceptancetest_100_2:
  runs-on: ubuntu-24.04
  if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
  steps:
    - uses: actions/checkout@v4
    - uses: ./.github/actions/checkout_mg5 
    - uses: ./.github/actions/restore-pip-cache
    - uses: ./.github/actions/restore_heptools
    - name: test one of the test test_loop_improved_reweighting
      run: |
          cd $GITHUB_WORKSPACE
          sudo apt install pigz
          ./tests/test_manager.py test_loop_improved_reweighting -pA -t0 -l INFO --debug

acceptancetest_101:
  runs-on: ubuntu-24.04
  if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
  steps:
    - uses: actions/checkout@v4
    - uses: ./.github/actions/checkout_mg5 
    - uses: ./.github/actions/restore_all
    - name: test one of the test test_loop_improved_reweighting
      run: |
          cd $GITHUB_WORKSPACE
          ./tests/test_manager.py test_nlo_output2 -pA -t0 -l INFO

acceptancetest_102:
  runs-on: ubuntu-24.04
  if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
  steps:
    - uses: actions/checkout@v4
    - uses: ./.github/actions/checkout_mg5 
    - uses: ./.github/actions/restore-pip-cache
    - uses: ./.github/actions/restore_heptools
    - name: Install Meson and Ninja
      run: |
         sudo apt-get update
         sudo apt-get install -y meson ninja-build
    - name: test one of the test test_loop_improved_reweighting
      run: |
          cd $GITHUB_WORKSPACE
          export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/home/runner/.cache/HEPtools/lhapdf6_py3/lib
          export PYTHONPATH=$PYTHONPATH:~/.cache/HEPtools/lhapdf6_py3/lib/python3.12/dist-packages
          export PYTHONPATH=$PYTHONPATH:~/.cache/HEPtools/lhapdf6_py3/lib/python3.10/dist-packages
          ./tests/test_manager.py test_nlo_reweighting -pA -t0 -l INFO

acceptancetest_104:
  runs-on: ubuntu-24.04
  if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
  steps:
    - uses: actions/checkout@v4
    - uses: ./.github/actions/checkout_mg5
    - uses: ./.github/actions/restore-pip-cache
    - uses: ./.github/actions/restore_all
    - name: test loop-induced gg>h cross-section
      run: |
          cd $GITHUB_WORKSPACE
          ./tests/test_manager.py test_loop_induced_ggh -pA -t0 -l INFO

acceptancetest_103_polarization:
  runs-on: ubuntu-24.04
  if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
  steps:
    # kept previous version (checkout@v2)
    - uses: actions/checkout@v2
    - name: test one of the test test_polarization_top_decay
      run: |
          cd $GITHUB_WORKSPACE
          cp input/.mg5_configuration_default.txt input/mg5_configuration.txt
          ./tests/test_manager.py test_polarization_top_decay -pA -t0 -l INFO

  acceptancetest_ewsudakov:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5 
      - uses: ./.github/actions/restore-pip-cache
      # Runs a set of commands using the runners shell
      - name: test one of the test test_tt_ewsudakov
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_ttbar_ewsudakov -pA -t0 -l INFO

  acceptancetest_uncovered_merged:
    # Previously uncovered acceptance tests in tests/acceptance_tests/test_cmd.py
    # related to the merged/ufo_aloha pipelines and decay-chain ordering
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache

      - name: test cmd uncovered merged/ufo_aloha/standalone tests
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_complex_mass_SA_merged test_import_model_v4_requires_debug test_standalone_cpp_fd_output_consistency test_ufo_aloha_merged -pA -t0 -l INFO


  acceptancetest_uncovered_madloop:
    # Previously uncovered acceptance test in tests/acceptance_tests/test_cmd_madloop.py
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_all

      - name: test ML gauge invariance acceptance test
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_ML_gauge_invariance_epem_ttx_all_modes -pA -t0 -l INFO

  acceptancetest_check_madevent_me_vs_standalone:
    # Previously uncovered acceptance test in tests/acceptance_tests/test_cmd_madloop.py
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5

      - name: test that ME of madevent are consistent with the standalone one
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py -pA TestStandaloneMadeventMatrixElementConsistency

  acceptancetest_check_mlm_rwgt:
    # Previously uncovered acceptance test in tests/acceptance_tests/test_MLM_reweight.py
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5

      - name: test MLM reweight factory cases (TestMLMReweight)
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py -pA TestMLMReweight


  acceptancetest_density:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5 
      - uses: ./.github/actions/restore-pip-cache
      # Runs a set of commands using the runners shell
      - name: test one of the test test_loop_improved_reweighting
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_standalone_density -pA -t0 -l INFO

      - name: test test_standalone_density_dd (ZZ density vs analytic decay)
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_standalone_density_dd -pA -t0 -l INFO

      - name: test test_standalone_density_uu
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_standalone_density_uu -pA -t0 -l INFO

      - name: test test_standalone_density_f2py (f2py density wrapper)
        run: |
            cd $GITHUB_WORKSPACE
            sudo pip install numpy
            which f2py
            echo "f2py_compiler = $(which f2py)" >> input/mg5_configuration.txt
            ./tests/test_manager.py test_standalone_density_f2py -pA -t0 -l INFO

