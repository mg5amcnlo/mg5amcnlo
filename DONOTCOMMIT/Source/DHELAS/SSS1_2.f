C     This File is Automatically generated by ALOHA 
C     The process calculated in this file is: 
C     1
C     
      SUBROUTINE SSS1_2(S1, S3, COUP, M2, W2,S2)
      USE ALOHA_OBJECT
      IMPLICIT NONE
      COMPLEX*16 CI
      PARAMETER (CI=(0D0,1D0))
      COMPLEX*16 CZERO
      PARAMETER (CZERO=(0D0,0D0))
      COMPLEX*16 COUP
      REAL*8 M2
      REAL*8 P2(0:3)
      TYPE(ALOHA) S1
      TYPE(ALOHA) S2
      TYPE(ALOHA) S3
      REAL*8 W2
      COMPLEX*16 DENOM
      INTEGER*4 I
      S2%P(:) = +S1%P(:)+S3%P(:)
      P2(:) = -S2 % P (:)
      S2 % W(:) = CZERO
      DENOM = COUP/(P2(0)**2-P2(1)**2-P2(2)**2-P2(3)**2 - M2 * (M2 -CI
     $ * W2))
      S2%W(5)= DENOM*CI * S3 % W(1)*S1 % W(1)
      CALL MULTIPLY_PROPAGATOR_FACTOR(S2,M2, S2)
      END


