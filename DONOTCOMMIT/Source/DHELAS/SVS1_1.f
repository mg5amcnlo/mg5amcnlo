C     This File is Automatically generated by ALOHA 
C     The process calculated in this file is: 
C     P(2,1) - P(2,3)
C     
      SUBROUTINE SVS1_1(V2, S3, COUP, M1, W1,S1)
      USE ALOHA_OBJECT
      IMPLICIT NONE
      COMPLEX*16 CI
      PARAMETER (CI=(0D0,1D0))
      COMPLEX*16 CZERO
      PARAMETER (CZERO=(0D0,0D0))
      COMPLEX*16 COUP
      REAL*8 M1
      REAL*8 P1(0:3)
      REAL*8 P3(0:3)
      TYPE(ALOHA) S1
      TYPE(ALOHA) S3
      COMPLEX*16 TMP5
      COMPLEX*16 TMP6
      TYPE(ALOHA) V2
      REAL*8 W1
      COMPLEX*16 DENOM
      INTEGER*4 I
      P3(:) = S3 % P (:)
      S1%P(:) = +V2%P(:)+S3%P(:)
      P1(:) = -S1 % P (:)
      S1 % W(:) = CZERO
      TMP5 = (P1(0)*V2 % W(1)-P1(1)*V2 % W(2)-P1(2)*V2 % W(3)-P1(3)*V2
     $  % W(4))
      TMP6 = (V2 % W(1)*P3(0)-V2 % W(2)*P3(1)-V2 % W(3)*P3(2)-V2 % W(4)
     $ *P3(3))
      DENOM = COUP/(P1(0)**2-P1(1)**2-P1(2)**2-P1(3)**2 - M1 * (M1 -CI
     $ * W1))
      S1%W(5)= DENOM*S3 % W(1)*(+CI*(TMP5)-CI*(TMP6))
      CALL MULTIPLY_PROPAGATOR_FACTOR(S1,M1, S1)
      END


