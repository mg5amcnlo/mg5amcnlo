C     This File is Automatically generated by ALOHA 
C     The process calculated in this file is: 
C     Metric(1,2)
C     
      SUBROUTINE VVS1_2(V1, S3, COUP, M2, W2,V2)
      USE ALOHA_OBJECT
      IMPLICIT NONE
      COMPLEX*16 CI
      PARAMETER (CI=(0D0,1D0))
      COMPLEX*16 CZERO
      PARAMETER (CZERO=(0D0,0D0))
      COMPLEX*16 COUP
      REAL*8 M2
      REAL*8 P2(0:3)
      TYPE(ALOHA) S3
      TYPE(ALOHA) V1
      TYPE(ALOHA) V2
      REAL*8 W2
      COMPLEX*16 DENOM
      INTEGER*4 I
      V2%P(:) = +V1%P(:)+S3%P(:)
      P2(:) = -V2 % P (:)
      V2 % W(:) = CZERO
      DENOM = COUP/(P2(0)**2-P2(1)**2-P2(2)**2-P2(3)**2 - M2 * (M2 -CI
     $ * W2))
      V2%W(1)= DENOM*(-CI )* V1 % W(1)*S3 % W(1)
      V2%W(2)= DENOM*(-CI )* V1 % W(2)*S3 % W(1)
      V2%W(3)= DENOM*(-CI )* V1 % W(3)*S3 % W(1)
      V2%W(4)= DENOM*(-CI )* V1 % W(4)*S3 % W(1)
      CALL MULTIPLY_PROPAGATOR_FACTOR(V2,M2, V2)
      END


C     This File is Automatically generated by ALOHA 
C     The process calculated in this file is: 
C     Metric(1,2)
C     
      SUBROUTINE VVS1_VSS1_SVS1_SSS1_2(V1, S3, COUP1, COUP2, COUP3,
     $  COUP4, M2, W2,V2)
      USE ALOHA_OBJECT
      IMPLICIT NONE
      COMPLEX*16 CI
      PARAMETER (CI=(0D0,1D0))
      COMPLEX*16 CZERO
      PARAMETER (CZERO=(0D0,0D0))
      COMPLEX*16 COUP1
      COMPLEX*16 COUP2
      COMPLEX*16 COUP3
      COMPLEX*16 COUP4
      REAL*8 M2
      REAL*8 P2(0:3)
      TYPE(ALOHA) S3
      TYPE(ALOHA) V1
      TYPE(ALOHA) V2
      TYPE(ALOHA) VTMP
      REAL*8 W2
      COMPLEX*16 DENOM
      INTEGER*4 I
      CALL VVS1_2(V1,S3,COUP1,M2,W2,V2)
      CALL VSS1_2(V1,S3,COUP2,M2,W2,VTMP)
      DO I = 1, 5
        V2 %W(I) = V2%W(I) + VTMP%W(I)
      ENDDO
      CALL SVS1_2(V1,S3,COUP3,M2,W2,VTMP)
      DO I = 1, 5
        V2 %W(I) = V2%W(I) + VTMP%W(I)
      ENDDO
      CALL SSS1_2(V1,S3,COUP4,M2,W2,VTMP)
      DO I = 1, 5
        V2 %W(I) = V2%W(I) + VTMP%W(I)
      ENDDO
      END


