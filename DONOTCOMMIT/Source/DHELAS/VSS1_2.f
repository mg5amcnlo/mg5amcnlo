C     This File is Automatically generated by ALOHA 
C     The process calculated in this file is: 
C     P(1,2) - P(1,3)
C     
      SUBROUTINE VSS1_2(V1, S3, COUP, M2, W2,S2)
      USE ALOHA_OBJECT
      IMPLICIT NONE
      COMPLEX*16 CI
      PARAMETER (CI=(0D0,1D0))
      COMPLEX*16 CZERO
      PARAMETER (CZERO=(0D0,0D0))
      COMPLEX*16 COUP
      REAL*8 M2
      REAL*8 P2(0:3)
      REAL*8 P3(0:3)
      TYPE(ALOHA) S2
      TYPE(ALOHA) S3
      COMPLEX*16 TMP3
      COMPLEX*16 TMP4
      TYPE(ALOHA) V1
      REAL*8 W2
      COMPLEX*16 DENOM
      INTEGER*4 I
      P3(:) = S3 % P (:)
      S2%P(:) = +V1%P(:)+S3%P(:)
      P2(:) = -S2 % P (:)
      S2 % W(:) = CZERO
      TMP3 = (P2(0)*V1 % W(1)-P2(1)*V1 % W(2)-P2(2)*V1 % W(3)-P2(3)*V1
     $  % W(4))
      TMP4 = (V1 % W(1)*P3(0)-V1 % W(2)*P3(1)-V1 % W(3)*P3(2)-V1 % W(4)
     $ *P3(3))
      DENOM = COUP/(P2(0)**2-P2(1)**2-P2(2)**2-P2(3)**2 - M2 * (M2 -CI
     $ * W2))
      S2%W(5)= DENOM*S3 % W(1)*(+CI*(TMP3)-CI*(TMP4))
      CALL MULTIPLY_PROPAGATOR_FACTOR(S2,M2, S2)
      END


