       double precision function photonpdfsquare(x1,x2)
       implicit none
       double precision x1,x2
       write(*,*) "WRONG gamma UPC linked"
       photonpdfsquare = 1.0
       stop 1
       return
       end
